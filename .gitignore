out/

# a demo is a root of its own that realizes boom and its closure under dep/
# as a path copy and plain clones; nothing there is source
/demo/*/dep/

# generated at runtime by demo/audio, not assets
*.wav
